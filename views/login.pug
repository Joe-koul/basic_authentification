html
   head
      title Signup
   body
      if(message)
         h4 #{message}
         form(action = "/login" method = "POST")
         input(name = "uname" type = "text" required placeholder = "Username")
         input(name = "pswd" type = "password" required placeholder = "Password")
         button(type = "Submit") Log in